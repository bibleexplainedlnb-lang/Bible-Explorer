Upgrade the system to ensure COMPLETE URL DISCOVERY, MIGRATION, and CONTROL.

---

🔴 PHASE 1 — DISCOVER ALL EXISTING URLS (CRITICAL)

Scan the entire project and collect ALL URLs that currently exist, including:

1. Database-driven articles
2. Static or hardcoded pages
3. Previously generated pages (even if not in DB)
4. Any accessible URL under:
   /topics/
   /questions/
   /guides/
   /bible-verses/
   or any other route

Also detect URLs from:

* internal links inside content
* sitemap (old + new)
* route generation logic

👉 OUTPUT:
Create a complete list of ALL URLs currently accessible on the site.

---

🟠 PHASE 2 — AUTO IMPORT INTO DATABASE

For every discovered URL:

1. Extract:

   * slug
   * title (from page)
   * content (if available)

2. Insert into "articles" table if NOT already present

3. Assign category based on URL structure:
   /topics/ → topics
   /questions/ → questions
   /guides/ → guides
   others → assign "uncategorized"

4. Set status = "draft" if content is weak or incomplete

👉 RESULT:
Every existing page must now exist inside Supabase.

---

🟢 PHASE 3 — ENABLE FULL CMS CONTROL

Now enforce:

1. Every page MUST come ONLY from database
2. If slug NOT found → return 404
3. Remove ALL fallback/static generation

---

🟢 PHASE 4 — ADMIN PANEL UPGRADE

Add:

1. FULL URL LIST VIEW
   → Show ALL articles (including imported ones)

2. Add filter:

* orphan pages (imported but not optimized)
* missing content
* duplicate slugs

3. Add bulk actions:

* delete selected URLs
* change category
* mark as draft/published

---

🟢 PHASE 5 — SITEMAP SYNC

1. Regenerate sitemap ONLY from DB
2. Ensure ALL imported URLs are included
3. Remove old Vercel or ghost URLs

---

🟢 PHASE 6 — CLEANUP TOOLS

Add buttons in admin:

* "Detect Orphan Pages"
* "Remove Non-DB URLs"
* "Fix Broken Links"
* "Rebuild Internal Linking"

---

🟢 FINAL RESULT

✔ Every URL is visible in admin
✔ No hidden pages
✔ No mismatch between sitemap and site
✔ Full control over all content
✔ Safe deletion without ghost pages

---

Build this without breaking existing working UI.
Ensure migration is safe and no data is lost.
